describe("Publish Course", () => {
  beforeEach(() => cy.visit("https://test-lms.erin.systems/"));
    cy.get('input[type="email"]').type('oyunchimeg');
    cy.get('input[type="password"]').type('Secret123');
    cy.get('button').click();

           });
   });

