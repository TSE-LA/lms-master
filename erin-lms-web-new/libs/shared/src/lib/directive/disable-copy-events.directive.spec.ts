import { DisableCopyEventsDirective } from './disable-copy-events.directive';

describe('DisableCopyEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableCopyEventsDirective();
    expect(directive).toBeTruthy();
  });
});
